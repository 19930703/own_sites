# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Own_Sites' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Own_Sites

  target 'Own_SitesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Own_SitesUITests' do
    # Pods for testing
  end

end
